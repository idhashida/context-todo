# 📡 Основные эндпоинты для BDTM API

Для разработки **BDTM API** на **Golang** с учетом методологии BDTM, важно спроектировать следующие основные эндпоинты.

---

## **1. Пользователи (`/users`)**
- **GET /users/{id}**: Получить информацию о пользователе.
- **POST /users**: Регистрация нового пользователя.
- **PUT /users/{id}**: Обновить информацию о пользователе.
- **DELETE /users/{id}**: Удалить пользователя.

---

## **2. Задачи (`/tasks`)**
- **GET /tasks**: Получить список всех задач текущего пользователя.
- **GET /tasks/{id}**: Получить информацию о конкретной задаче.
- **POST /tasks**: Создать новую задачу.
- **PUT /tasks/{id}**: Обновить информацию о задаче (например, статус, описание).
- **DELETE /tasks/{id}**: Удалить задачу.
- **PATCH /tasks/{id}/status**: Изменить статус задачи.

---

## **3. Сценарии Given-When-Then (`/scenarios`)**
- **GET /scenarios/{task_id}**: Получить сценарии для конкретной задачи.
- **POST /scenarios**: Создать новый сценарий для задачи.
- **PUT /scenarios/{id}**: Обновить сценарий.
- **DELETE /scenarios/{id}**: Удалить сценарий.

---

## **4. Контексты (`/contexts`)**
- **GET /contexts**: Получить список всех контекстов для текущего пользователя.
- **GET /contexts/{id}**: Получить информацию о конкретном контексте.
- **POST /contexts**: Создать новый контекст.
- **PUT /contexts/{id}**: Обновить информацию о контексте.
- **DELETE /contexts/{id}**: Удалить контекст.

---

## **5. Статусы (`/statuses`)**
- **GET /statuses**: Получить список всех доступных статусов.
- **GET /statuses/{id}**: Получить информацию о конкретном статусе.
- **POST /statuses**: Добавить новый статус.
- **PUT /statuses/{id}**: Обновить информацию о статусе.
- **DELETE /statuses/{id}**: Удалить статус.

---

## **6. Критерии успешности (`/success_criteria`)**
- **GET /success_criteria/{task_id}**: Получить критерии успешности для задачи.
- **POST /success_criteria**: Добавить критерий успешности для задачи.
- **PUT /success_criteria/{id}**: Обновить критерий успешности.
- **DELETE /success_criteria/{id}**: Удалить критерий успешности.

---

## **7. История изменений задач (`/task_history`)**
- **GET /task_history/{task_id}**: Получить историю изменений для задачи.
- **GET /task_history/user/{user_id}**: Получить историю всех задач конкретного пользователя.
- **POST /task_history**: Добавить запись об изменении задачи (автоматически при изменении задачи).

---

## **8. Комментарии (`/comments`)**
- **GET /comments/{task_id}**: Получить список всех комментариев для задачи.
- **POST /comments**: Добавить комментарий к задаче.
- **DELETE /comments/{id}**: Удалить комментарий.

---

## **9. Аутентификация и авторизация (`/auth`)**
- **POST /auth/login**: Вход в систему.
- **POST /auth/register**: Регистрация нового пользователя.
- **POST /auth/logout**: Выход из системы.

---

## **10. Дополнительные эндпоинты**
- **GET /health**: Проверка состояния сервиса (пинг).
- **GET /stats**: Получить статистику по задачам пользователя (например, сколько задач выполнено, сколько в процессе и т.д.).

